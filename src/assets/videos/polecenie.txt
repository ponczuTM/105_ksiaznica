for i in {1..4}; do
  ffmpeg -i "${i}.mp4" -c:v libx264 -preset slow -crf 18 -pix_fmt yuv420p \
  -g 120 -keyint_min 120 -sc_threshold 0 \
  -c:a aac -b:a 192k -movflags +faststart "${i}_optimized.mp4"
done